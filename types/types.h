#include <iostream>
#include <queue>
#include <stack>
#include <unordered_map>
#include <vector>

typedef std::string string_type;
typedef std::vector<int> int_array;

typedef std::unordered_map<int, int> int_int_hashmap;
typedef std::queue<int> int_queue;
